



``` mermaid
erDiagram
    news {
        varchar(30) realkey PK
        varchar(255) title
        date date
        time time
        varchar(8) media
        time realtime
    }

    stock {
        char(6) ticker PK
        varchar(8) corp_code
        varchar(40) name
        int market_cap
    }

    news_stock {
        varchar(30) realkey PK, FK
        char(6) ticker PK, FK
    }

    news_snapshot {
        varchar(30) realkey PK, FK
        char(6) ticker FK
        int close_pre
        int high
        int low
        int current
        float high_rate
        float current_rate
        int market_cap
        tinyint margin_rate
        float trading_value
    }

    news_backtest {
        varchar(30) realkey PK, FK
        int high_daily
        time high_daily_time
        int low_daily
        time low_daily_time
        float max_daily
        float min_daily
        int high_10m
        time high_10m_time
        int low_10m
        time low_10m_time
        float max_10m
        float min_10m
        float trading_value_1m
    }

    disclosure {
        varchar(30) realkey PK, FK
        varchar(122) report
        varchar(14) rcept_no
    }

    news ||--o{ news_stock : "관계"
    stock ||--o{ news_stock : "관계"
    news ||--|| news_snapshot : "관계"
    stock ||..o{ news_snapshot : "관계"
    news ||--|{ news_backtest : "관계"
    stock ||--|{ news_backtest : "관계"
    news ||--|| disclosure : "관계"
```

- 🚨 (경고/알림)
- ✅ (성공)
- ❌ (실패)
- 📈 (차트/주가)
- 📰 (뉴스)
- 💡 (힌트)
- 🛒 (매수/구매)
- 🏦 (금융/은행)
- 🕵️ (탐색/분석)
- 🧩 (조각/구성)
- 🏷️ (라벨/태그)
- 📝 (기록/로그)
- 🕑 (시간)
- 🔗 (연결)
- 🧠 (AI/머신러닝)
- 🦾 (자동화/로봇)
