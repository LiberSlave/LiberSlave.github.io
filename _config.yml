theme: jekyll-theme-hacker
title: 내 포트폴리오
description: 방문해주셔서 감사합니다.


defaults:
  -
    scope:
      path: "" # 저장소 전체의 모든 파일에 적용
      type: "posts" # 또는 "pages" 등, 보통은 path만으로 충분합니다.
    values:
      layout: "default
